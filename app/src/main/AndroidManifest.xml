<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sportjournal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportJournal">

        <!-- Activité principale : liste des séances -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Détail d'une séance — parent = MainActivity -->
        <activity
            android:name=".SeanceDetailActivity"
            android:label="Détail séance"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

        <!-- Créer une nouvelle séance — parent = MainActivity -->
        <activity
            android:name=".NouvelleSeanceActivity"
            android:label="Nouvelle séance"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

    </application>

</manifest>